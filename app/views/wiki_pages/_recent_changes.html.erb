<section>
  <h1>Recent Changes (<%= link_to "all", booru_wiki_pages_path(Booru.current, (:order => "time") %>)</h1>
  <ul>
    <% WikiPage.recent.each do |page| %>
      <li><%= link_to page.pretty_title, booru_wiki_page_path(Booru.current, page) %></li>
    <% end %>
  </ul>
</section>
